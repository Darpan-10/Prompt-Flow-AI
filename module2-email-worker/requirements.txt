# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Pydantic v2
pydantic==2.5.0
pydantic-settings==2.1.0

# Gmail API (OAuth2 Service Account — headless)
google-auth==2.23.4
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1

# Kafka (idempotent producer)
confluent-kafka==2.3.0

# Redis (deduplication)
redis==5.0.1

# AWS (S3 multipart, Secrets Manager, CloudWatch)
boto3==1.33.0
botocore==1.33.0

# Database
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ClamAV (ZINSTSTREAM via raw socket — no extra lib needed)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
